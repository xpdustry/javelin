package fr.xpdustry.javelin;

public final class JavelinSocketTest {

  /*
  private TestJavelinAuthenticator authenticator;
  private JavelinClientSocket client;
  private JavelinServerSocket server;

  @BeforeEach
  void setup() {
    authenticator = new TestJavelinAuthenticator();
    client = new JavelinClientSocket(URI.create("ws://localhost:2000"), "bob", "bob".toCharArray(), 1);
    server = new JavelinServerSocket(2000, 1, authenticator);
  }

  @Test
  void test_simple_connection() {
    authenticator.addUser("bob", "bob");
    server.start();
    client.start();
    Assertions.ass
  }
   */
}
